{
  "name": "word-template",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.6",
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8",
    "@sap/hana-client": "^2.22.27",
    "easy-template-x": "^4.1.1"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch --profile hybrid",
    "deploy:hana":"cds deploy --to hana --profile hybrid"
  },
  "cds": {
    "requires": {
      "auth": "mocked"
    }
  }
}
